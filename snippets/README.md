# 📦 Code Snippets Collection

Reusable and interview-ready C++ snippets used across problems and patterns.

These are **modular**, **well-commented**, and **ready-to-paste** during interviews or contests. Each snippet solves a common sub-problem or implements a core STL/data structure behavior.

## 📎 Snippets Reference

Reusable C++ code snippets for common DSA patterns. Use these as starting points during problem-solving or interview prep.

| #  | Snippet Name              | Description                                                                 | 📎 View                      |
|----|---------------------------|-----------------------------------------------------------------------------|------------------------------|
| 1  | `min_heap.cpp`            | Custom `std::priority_queue` for min-heap use cases                         | [View](./min_heap.cpp)       |
| 2  | `bucket_sort.cpp`         | Bucket sort skeleton for frequency/grouping-based problems                  | [View](./bucket_sort.cpp)    |
| 3  | `hashmap_freq.cpp`        | Standard pattern for counting frequencies using `unordered_map`            | [View](./hashmap_freq.cpp)   |


## 📌 Usage Tips

- Use `#include "utils.h"` in your problem solution file to include shared/common utilities.
- Snippets are **not bloated** — kept minimal for fast writing and understanding.
- Most used in `Hashing`, `Sliding Window`, `Heap`, and `Bucket` pattern problems.

---

### ✅ Contributing New Snippets

If you build a reusable pattern while solving a problem, extract it and add it here as a named `.cpp` file with:
- Problem link (in comments)
- What it solves
- Time & Space complexity
- Edge-case resilience (if needed)

---
